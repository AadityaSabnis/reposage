# Versions below are the set verified together on Python 3.12
# (all tests pass, eval runs end-to-end).

# ── Web framework ────────────────────────────────────────────────
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32

# ── HTTP client (LLM calls: Ollama + hosted) ─────────────────────
httpx==0.28.1

# ── Embeddings + vector store ────────────────────────────────────
# (sentence-transformers pulls a compatible torch/transformers itself)
sentence-transformers==5.6.0
faiss-cpu==1.14.3
numpy==2.5.0

# ── AST-aware code chunking ──────────────────────────────────────
tree-sitter==0.21.3
tree-sitter-languages==1.10.2

# ── Local file watching (dev incremental trigger) ────────────────
watchdog==6.0.0

# ── Tests ────────────────────────────────────────────────────────
pytest==9.1.1
